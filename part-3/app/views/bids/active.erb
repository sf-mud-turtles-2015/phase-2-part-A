<h3>Active Items</h3>

<ul>
  <% @items.each do |item|  %>
    <li>
      <a href="/items/<%= item.id %>"><%= item.description %></a>
      <p>Current Bid: <%= item.bids.last.amount %></p>
    </li>
  <% end %>
</ul>
